% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_temp_program.R
\name{plot_temp_program}
\alias{plot_temp_program}
\title{Plot temperature program over time}
\usage{
plot_temp_program(
  data,
  time_col = "time",
  temp_col = NULL,
  stage_col = "stage",
  stage_labels = NULL,
  stage_colors = NULL,
  bg_alpha = 0.55,
  line_col = "black",
  line_lwd = 2,
  label_cex = 1,
  draw_boundaries = TRUE,
  main_title = "Temperature Program (TGA)",
  ...
)
}
\arguments{
\item{data}{data.frame containing the temperature program.}

\item{time_col}{column name for time (in minutes); defaults to `time`.}

\item{temp_col}{column name for temperature (in Celsius). If `NULL`, the
function looks for `temp_C` and then `temp`.}

\item{stage_col}{column name that identifies stage membership.}

\item{stage_labels}{labels to show beneath each stage band. Defaults to
Roman numerals ordered by stage start time.}

\item{stage_colors}{fill colours for stage bands. Recycled if fewer colours
are provided than stages.}

\item{bg_alpha}{alpha applied to the stage fill colours.}

\item{line_col}{colour for the temperature trace.}

\item{line_lwd}{line width for the temperature trace.}

\item{label_cex}{scaling for the stage labels.}

\item{draw_boundaries}{logical; if `TRUE`, dashed lines are drawn at stage
boundaries.}

\item{main_title}{title placed above the plot. Set to `""` to suppress.}

\item{...}{additional arguments passed to [graphics::plot()].}
}
\value{
Invisibly returns a data.frame with start/end times and
  temperature ranges for each stage.
}
\description{
Draw a temperature versus time profile with shaded bands for each program
stage. The default layout matches the six-stage program supplied in
`beech`, but any dataset with time, temperature, and stage columns can be
used.
}
\examples{
data(beech)
plot_temp_program(beech)

}
