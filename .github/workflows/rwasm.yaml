name: Build R-WASM binaries

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: write
  repository-projects: read
  pages: write
  id-token: write

jobs:
  release-wasm:
    uses: r-wasm/actions/.github/workflows/release-file-system-image.yml@v2
